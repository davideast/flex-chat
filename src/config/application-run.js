export function ApplicationRun($rootScope, $location) {
  $rootScope.$on("$routeChangeError", function(event, next, previous, error) {
    if (error === "AUTH_REQUIRED") {
      $location.path("/login");
    }
  });  
}
ApplicationRun.$inject = ['$rootScope', '$location'];
